#ifndef TPROPERTY_H
#define TPROPERTY_H

/*template <class typ, class parnt>
class TProperty
{
public:
	typ Value;

	virtual typ get() = 0;
	virtual void set(typ value) = 0;

	inline parnt GetThis(void* curthis)
	{
		return (parnt)curthis;
	}

	inline TProperty<typ,parnt>& operator = ( const typ& val)
	{
		set(val);
	}

	inline operator typ const& () const
	{
		return get();
	}
};*/

#endif